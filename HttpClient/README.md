# Selenium Utilities : HTTP client 

Selenium has builtin apache http client. We will use that to send receive req. 
  
# Target 
Use selenium inside apache http client to send and recieve requests 
